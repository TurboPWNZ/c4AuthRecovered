#include "WinMain.h"

int WINAPI wWinMain(
    HINSTANCE hInstance,
    HINSTANCE hPrevInstance,
    PWSTR lpCmdLine,
    int nCmdShow)
{
    return RunApplication(
        hInstance,
        hPrevInstance,
        lpCmdLine,
        nCmdShow);
}
